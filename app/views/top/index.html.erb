Result
EDIT ON
<div class="header"></div>
  <input type="checkbox" class="openSidebarMenu" id="openSidebarMenu">
  <label for="openSidebarMenu" class="sidebarIconToggle">
    <div class="spinner diagonal part-1"></div>
    <div class="spinner horizontal"></div>
    <div class="spinner diagonal part-2"></div>
  </label>
  <div id="sidebarMenu">
    <ul class="sidebarMenuInner">
       <li> <%= link_to '新規登録', new_user_registration_path %> <li>
       <li> <%= link_to 'ログイン', new_user_session_path %> <li>
       <li><% if user_signed_in? %> 
            <%= link_to 'プロフィール', user_path(current_user.id)%>
        <% else %>
             <%= link_to "プロフィール", new_user_session_path %> 
        <% end %>
      <li>
      <li><% if user_signed_in? %> 
            <%= link_to 'メッセージ', rooms_path %>
        <% else %>
             <%= link_to "メッセージ", new_user_session_path %> 
        <% end %>
      <li>
      

      
      <li><% if user_signed_in? %>
           <% if unchecked_notifications.any? %>
            <span>●</span>
           <% end %>
           <%= link_to "通知", notifications_path%>
          <% else %>
           <%= link_to "通知", notifications_path%>
          <% end %>
      <li>

      <li><% if user_signed_in? %> 
            <%= link_to "新規サービス登録", courses_new_path %>
        <% else %>
            <%= link_to "新規サービス登録", new_user_session_path %> 
        <% end %>
      <li>
      <li><%= link_to "サービス一覧", "/courses" %><li>
      <li><% if user_signed_in? %> 
            <%= link_to "要望登録", demands_new_path %>
        <% else %>
            <%= link_to "新規サービス登録", new_user_session_path %> 
        <% end %>
      <li>
      
      
    
      <li><%= link_to "要望一覧", "/demands" %><li>
      
      
    </ul>
  </div>
  <div id='center' class="main center">
    <div class="mainInner">
      <% @course.select(:user_id).distinct.each do |course| %>
 <p><%=link_to course.user.name,show2_user_path(course.user.id) %></p>
 <p><%= course.user.declaration %></p>
 <% course.user.tag_list.each do |tag| %>
  <span class="badge badge-primary"><%= tag %></span>
<% end %>
 <% end %>
    </div>
    <div class="mainInner">
      <div>今晩わ</div>
    </div>
    <div class="mainInner">
      <div>PURE CSS SIDEBAR TOGGLE MENU</div>
    </div>
  </div>